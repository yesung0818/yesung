#include <stdio.h>
void func(int a, int b){
    int temp;
    temp=a;
    a=b;
    b=temp;
    printf("func를 실행한 후의 x와 y의 값은?-> %d,%d\n",a,b);
}
int main(){
    int x=100,y=200;
    func(x,y);
    printf("func를 실행한 후의 x와 y의 값은?-> %d,%d\n",x,y);
    return 0;
}
